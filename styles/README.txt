We got permission to have three CSS files, one for all the pages (global), one for the main page (index), and one for the sub-pages (album). So don't worry about us having three .css files.

-- Update 3/28 (Kaleb)

I moved the album CSS to global so we don't need the extra album.css file